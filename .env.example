# Optional. Leave unset: the dev server proxies /api to http://localhost:8787,
# and in production the worker serves the app and the API from one origin.
# Only set this if you host the app somewhere other than the worker.
# VITE_WORKER_URL=https://space-reset-worker.<your-subdomain>.workers.dev
