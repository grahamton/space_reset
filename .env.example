# Deployed worker origin. Leave unset for local dev — the Vite dev server
# proxies /api to http://localhost:8787 instead (see vite.config.js).
VITE_WORKER_URL=https://space-reset-worker.<your-subdomain>.workers.dev
