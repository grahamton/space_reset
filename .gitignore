node_modules
dist

# Secrets — the worker reads ANTHROPIC_API_KEY from here in local dev
.env
.env.*
!.env.example
worker/.dev.vars

# Tooling output
coverage
.wrangler
*.local

.DS_Store
.claude/launch.json
.claude/settings.local.json

# Local-only eval photos (may be licensed/watermarked; never commit)
eval-photos/

# copy-check output: generated missions and TypeSafe answers for those photos
eval-results/
